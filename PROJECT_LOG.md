# Project Log

## 2026-04-02 18:06:32 +0200
- What was improved: Added explicit support-track completion telemetry to the progress hub. `game/logic/progress.ts` now derives a `nonCombat` summary alongside the existing recommendation, including completed non-combat quest count versus total, the next support-chain category (`skill` or `exploration`), and a typed blocker summary (`ready`, `active`, `skill`, `player`, `prerequisite`, `resource`, `complete`). `ui/screens/ProgressScreen.tsx` now renders that support-track status as its own card with progress, category, blocker label, and blocker detail, while `ui/components/game/CompletionSummaryCard.tsx` mirrors the same information on the dashboard so players can compare the combat and support lanes without drilling into quest lists. Regression coverage in `game/logic/progress.test.js` now locks the new summary against ready, active, and skill-gated support-chain states.
- Why it was improved: The support recommendation layer had become actionable, but it still hid the ledger state behind a single CTA. Players could jump to the right quest or skill, yet they still lacked a quick read on how far the support track was from completion or what class of gate was currently slowing it down. That made the combat lane feel more measurable than the automation-heavy non-combat lane.
- Strategic direction chosen: Continue deepening the existing completion architecture instead of branching into a new system. This pass stayed data-first: one richer pure summary object, thin UI rendering on the two existing completion surfaces, and no save, routing, or game-rule changes.
- Next potential vector: Add blocker-specific progress details for support gates, such as current-versus-required skill levels or prerequisite-quest completion percentages, so the support card can show not just the blocker type but exactly how close the player is to clearing it.

## 2026-04-02 17:05:25 +0200
- What was improved: Tightened the completion advisor’s execution layer so recommendations now land on the exact thing they reference instead of only the broad destination surface. `ui/components/game/completion-recommendation-action.ts` now carries optional route params for quest-focused recommendations and deep-links skill-gated advice straight into the matching `/skill/[skillId]` route. `app/quests.tsx` and `ui/screens/ProgressScreen.tsx` now forward a `questId` spotlight into the quests tab, while the new pure helper `ui/screens/quests-focus.ts` resolves where that quest currently lives. `ui/screens/QuestsScreen.tsx` uses that resolver to auto-open completed history when needed and `QuestCard` can visually mark the guided target, so the player lands on a concrete next action rather than another scan step. Regression coverage was expanded for both the action mapper and the new quest spotlight logic, and `package.json` now includes the new UI-focused test in the pure logic test command.
- Why it was improved: The recommendation engine had become meaningfully accurate, but one friction point remained: the CTA still dumped the player onto a general tab and forced another round of manual searching. That gap weakens the project’s automation-first design principle because the guidance layer should remove interpretation work, not merely relocate it.
- Strategic direction chosen: Keep deepening the progress hub instead of branching into a new feature. This pass stayed within the existing architecture: a richer action adapter, thin route-param plumbing, one small pure resolver, and lightweight UI emphasis with no save changes and no new game rules.
- Next potential vector: Surface explicit support-track completion metrics next to the non-combat recommendation, such as completed non-combat quests versus total and the currently blocking prerequisite category, so players can compare both progression lanes at a glance.

## 2026-04-02 16:10:28 +0200
- What was improved: Expanded the completion recommendation system into a two-lane planner instead of a single late-combat pointer. `game/logic/progress.ts` still derives the primary final-contract recommendation, but it now also derives a `nonCombatRecommendation` that tracks the next unfinished non-combat quest chain, including prerequisite-quest fallbacks, concrete remaining objectives for active support quests, and skill-gate recommendations when the next quest is locked behind a level threshold. The quest unlock evaluator inside the completion logic now understands skill and resource gates in addition to quest/player gates, so completion status matches the actual quest data. `store/selectors.ts` now feeds the extra skills/resources state into the pure completion selector, and both `CompletionSummaryCard` and `ProgressScreen` now render a second recommendation surface for the support track. CTA routing was extended so these new recommendation kinds can start quests directly or send the player back to skills training, and regression coverage was added for both the pure logic and the action mapper.
- Why it was improved: The previous recommendation work solved navigation friction, but the ledger still read like a combat-only feature. Once the final-contract lane was visible, the next weakness was obvious: the game had no equally concrete guidance for the rest of the completion surface, and some quest locks were still being evaluated with incomplete condition support. That made the support systems feel secondary in a project that is supposed to reward automation and broad long-term progression.
- Strategic direction chosen: Deepen the existing progress hub instead of adding another top-level system. This pass kept the architecture intact: richer pure recommendation derivation, a slightly wider selector input, and thin UI surfaces that render the second lane without changing save shape or game rules.
- Next potential vector: Make the new non-combat lane even more execution-oriented by jumping to the exact skill detail or quest subset it references, and consider surfacing support-track progress percentages so the player can compare combat and non-combat completion at a glance.

## 2026-04-02 15:16:57 +0200
- What was improved: Closed the loop on the new completion recommendation system by making it actionable and finishable. `game/data/completion-goals.data.ts` now caps the completion combat target at 99 instead of an unreachable 120, matching the combat progression ceiling. `game/logic/progress.ts` now tags every recommendation with a focus area so the UI can route the player to the right system. A new `completion-recommendation-action` helper plus `useCompletionRecommendationAction` hook map those recommendations into concrete actions: start the next contract immediately, preselect the correct combat zone/enemy, or jump to the relevant training surface. The dashboard `CompletionSummaryCard` and the Progress screen recommendation panel now expose CTA buttons instead of passive copy, and regression coverage was added for both the recommendation metadata and the action mapping.
- Why it was improved: The previous pass added good late-game guidance, but it still had two structural gaps. First, the ledger could never truly complete because one target exceeded the game’s real combat cap. Second, the guidance still forced the player to manually translate advice into navigation and setup steps. That undercut the project’s automation-first design bias.
- Strategic direction chosen: Refine the existing progress hub before expanding it further. This work stayed entirely inside the current architecture: one corrected balance constant, richer pure recommendation metadata, a small UI-side action adapter, and thin CTA wiring in existing surfaces.
- Next potential vector: Extend the recommendation engine beyond the final-contract chain so it can choose the highest-leverage non-combat completion target when the ledger’s combat hunts are already exhausted, such as the best remaining quest deficit or total-kill milestone.

## 2026-04-02 14:10:42 +0200
- What was improved: Added a real recommendation layer to the Progress hub and surfaced it on the dashboard. `game/logic/progress.ts` now derives a single next-step recommendation from live completion state: start the next final contract, continue the active hunt with concrete remaining objectives, or train combat when the next hunt is still level-gated. `ui/screens/ProgressScreen.tsx` now promotes that recommendation near the top of the Completion tab, while a new `CompletionSummaryCard` plus dedicated `/progress` route puts the same guidance directly on the dashboard for faster long-session planning. Regression coverage was added for all three recommendation branches, and the safe-area metric tests were aligned with the new notch spacing contract so the full pure-logic suite stays green.
- Why it was improved: The Progress hub had become descriptive but still not directive. It could tell the player where they stood, but it still forced them to translate status into the next useful action. That is friction in exactly the part of an idle systems game that should feel most automated and legible.
- Strategic direction chosen: Keep deepening progression guidance rather than adding another standalone system. The work stayed inside the existing architecture: pure derived logic, selector plumbing, and thin UI surfaces that render one recommendation object without changing save shape or combat rules.
- Next potential vector: Extend the recommendation layer beyond final-contract progression so it can also choose the best next non-combat completion target, such as the highest-value quest deficit, level gap, or kill milestone when the final contract chain is already cleared.

## 2026-04-02 13:13:04 +0200
- What was improved: Stabilized and upgraded the new Progress hub’s completion layer. `game/logic/progress.ts` now derives explicit final-contract statuses (`locked`, `available`, `active`, `completed`) and per-hunt contract kill guidance from live quest state, so the completion view can show what is actually actionable instead of only raw totals. `ui/screens/ProgressScreen.tsx` now surfaces those statuses directly on final contracts and hunts, including contract kill progress and remaining kills. Verification also got formalized: `package.json` now includes a working `test:logic` command that runs the pure Node-safe test suite through `sucrase` plus path-alias registration.
- Why it was improved: The in-flight Progress screen had crossed the line from “promising” to “fragile”. It exposed high-level completion numbers, but it did not tell the player which final contract was currently open, which hunt was actively progressing, or how much work remained. On top of that, the repo’s pure logic tests were not runnable through a single reliable command, which blocked disciplined iteration on exactly this kind of systems UI.
- Strategic direction chosen: Finish the partially built Progress feature before expanding into a new progression system. The work stayed inside the existing data-first architecture: pure derived logic, thin UI rendering of that logic, and a repeatable verification path for future autonomous passes.
- Next potential vector: Add a dedicated late-game recommendation layer on top of completion state, such as “next best completion target” or “best hunt for the currently available final contract”, so the dashboard can direct long idle sessions instead of only reporting status.

## 2026-04-02 12:08:54 +0200
- What was improved: Extended the combat farm advisor with quest-aware routing. `game/logic/combat/planning.ts` now derives active kill-quest targets from quest state, scores matching enemies by projected time-to-completion, and exposes per-enemy quest metadata alongside the existing XP/value/safety projections. `CombatHuntView` gained a `Quest` focus chip when active kill quests exist, quest-specific advisor copy, and a selected-target quest progress panel. `CombatZoneCard` now marks quest-target enemies directly inside the picker, and regression coverage was added for quest-priority ranking plus overlapping quest progress on the same enemy.
- Why it was improved: The planner had already become strong at generic optimization, but it still ignored the game’s directed progression layer. Players with active combat quests had to manually translate quest objectives into route choices, which is friction in exactly the part of the game that should feel most automated. This closes that gap without changing combat rules or save shape.
- Strategic direction chosen: Keep deepening pre-run decision support instead of adding new combat mechanics. The change stays within the existing architecture: pure planning logic, selector plumbing, and lightweight UI surfaces that explain the recommendation rather than inventing new systems.
- Next potential vector: Add a drop-chasing planner focus so players can target a specific item and have the advisor recommend the fastest safe route for that drop.

## 2026-04-02 11:10:32 +0200
- What was improved: Turned combat route comparisons into an actionable advisor. Added pure focus-aware farm ranking in `game/logic/combat/planning.ts` with three planning goals: XP, coin value, and safety. The new planner ranks viable routes ahead of lethal traps, picks the best enemy per zone for the chosen focus, and exposes a single recommended route. Hunt Setup now includes a `Farm Advisor` panel with focus chips, a one-tap `Use Route` action, and recommendation details. Zone cards now highlight the top zone for the current focus and mark the recommended enemy inside the picker. Added regression coverage for focus ranking, lethal-route deprioritization, and per-zone safe-vs-XP summaries.
- Why it was improved: The previous pass made route quality visible, but players still had to manually scan the list and mentally optimize it. That left the strongest farm opaque in exactly the place where an idle systems game should be most helpful: choosing the next long-running route. This closes the loop from raw metrics to actual player decision support.
- Strategic direction chosen: Continue deepening combat planning instead of adding new combat mechanics. Keep the game logic pure and data-driven, then let the UI surface a recommendation layer on top of those existing projections without touching save shape or combat rules.
- Next potential vector: Extend the advisor with player-selectable route goals outside combat, such as quest-target farming or specific drop chasing, so planning can optimize for progression objectives instead of only generic XP/value/safety.

## 2026-04-02 (automated)
- What was improved: Surfaced farm comparison metrics directly on each zone card and enemy picker. Every unlocked zone card now shows a compact strip — XP/min, value/min (coins), and a colour-coded risk badge (SAFE / STEADY / RISKY / LETHAL) — visible at a glance without selecting the zone. When a zone is expanded, the enemy picker shows per-enemy XP/min and value/min inline so players can compare all targets before committing. A new `useAllZoneProjections` selector in `store/selectors.ts` computes projections for all unlocked zones and every reachable enemy in a single memoised pass, using the existing `estimateCombatRoute` pure function. Data flows down as optional props to `CombatZoneCard`, keeping locked zones unchanged.
- Why it was improved: The previous pass added the full route projection card inside Hunt Setup, but that card only populated once a specific target was already selected. Players still had to drill into each zone card to understand whether a new zone was an upgrade. This closes that gap: all farm options are comparable at a glance from the zone list before any selection is made.
- Strategic direction chosen: Continue extending pre-run decision visibility outward — from the route projection card (inside setup) to the zone list (outside setup). No new combat rules, no save changes. One new selector, prop additions to an existing card component, and pure UI rendering of already-computed data.
- Next potential vector: Add a "Best Farm" highlight or sort-by-value/XP capability to the zone list so the top unlocked farm auto-surfaces rather than requiring the player to scan all cards. Alternatively, add a compact combat stats summary to the Dashboard active session card showing current XP/min and value/min for the active auto-fight route.

## 2026-04-02 10:09:05 +0200
- What was improved: Extended combat route planning with expected loot economics. `estimateCombatRoute` now projects average coin rolls, expected vendor value from combat drops, total value per kill, value per minute, and the top three value-driving drops for the selected target. Hunt Setup now surfaces those metrics directly in the Route Projection card so players can weigh profit alongside XP pace and sustain. Added regression coverage to lock the new planning guarantees: profit per minute scales with kill pace, expected loot value is computed from the drop table, and notable drops are sorted by expected value.
- Why it was improved: The previous planning pass made combat survivability legible, but target choice was still economically blind. Idle combat needs more than "can I live here?" Players also need "is this farm worth the time?" This closes that gap using existing data tables and vendor values without adding any new runtime combat rules.
- Strategic direction chosen: Keep deepening pre-run decision support rather than expanding combat mechanics. The change stays aligned with the project’s systems-first bias: one pure planning extension, existing store selectors, and a compact UI readout. No save migration, no loot-table rewrite, no new automation complexity.
- Next potential vector: Surface route and profit comparisons directly on each zone/enemy card so the best unlocked farm is visible before the player drills into a specific target.

## 2026-04-02 09:57:00 +0200
- What was improved: Added combat route projections to Hunt Setup so the selected enemy now shows a forward-looking run estimate instead of only unlock text. Introduced a pure `estimateCombatRoute` helper in `game/logic/combat/planning.ts` that projects kill pace, XP per minute, incoming pressure, stocked healing coverage, and a run risk rating using the current combat stats, pet support, regen, and bag food. Added `useCombatRouteProjection` to keep the selector boundary clean and wired the result into a new Route Projection block on the combat setup screen. Added regression coverage for three planning guarantees: pets improve pace, stocked food extends sustain, and obviously overmatched targets are marked lethal.
- Why it was improved: Combat had become much more legible after the rhythm/log work, but pre-run decisions were still blind. Players could choose a zone and target, yet had no practical read on whether the route was efficient, sustainable, or a trap. This closes that gap with data-first guidance that supports the idle-planning fantasy without changing any combat rules.
- Strategic direction chosen: Extend combat readability from reactive feedback into proactive planning. The implementation stayed inside the existing architecture: one pure logic helper, one selector hook, and one compact UI surface in Hunt Setup. No save shape changes, no combat balance changes, no new automation rules.
- Next potential vector: Add value-per-kill projections to each target, combining XP, coins, and notable drops so players can choose the best farm for profit as well as survival.

## 2026-04-01 22:11:09 +0200
- What was improved: Added an in-screen combat feedback layer. CombatScreen now renders a `Combat Rhythm` card with live player/enemy/pet attack timers and a rolling `Combat Log` card with newest-first high-signal entries and session kill count. Introduced transient `combatFeedback` store state plus a pure `applyCombatFeedbackEvents` mapper to translate live combat events into readable transcript lines without persisting them. Wired combat feedback reset into boot/load/reset paths, and added regression coverage for log formatting, entry caps, crit text, and `sessionStartedAt` initialization/preservation.
- Why it was improved: Combat mechanics were functioning, but the loop still felt opaque. Players could not quickly answer who attacks next or what just happened in a long auto-fight run. This pass improves readability without changing formulas, and it hardens the UI-only state boundary so transient combat feedback does not drift into persistence behavior.
- Strategic direction chosen: Finish and stabilize the active combat-feedback frontier before expanding adjacent systems. The work stayed inside the store/UI boundary, reused existing tick events and timestamps, and avoided new combat mechanics or save-shape changes.
- Next potential vector: Add zone efficiency guidance to combat setup, such as projected damage intake, kill pace, and food consumption per zone/enemy selection so players can make better idle-planning decisions before starting a run.

## 2026-03-16 (automated) — session 11
- What was improved: (1) Replaced the two remaining `Math.random()` crit rolls in `game/logic/combat/tick.ts` (player attack crit and enemy attack crit) with the seeded RNG path already used everywhere else in the codebase. `createRng(state.rngSeed)` is now called at the top of `processCombatTick` and both critical-hit rolls use `rollChance(rng, chance)` instead of `Math.random()`. (2) Added a 5-quest Herblore chain to `game/data/quests.data.ts`: `first_brew` (gain 300 herblore XP + 1 attack_potion, unlocks after `first_steps`), `junior_alchemist` (herblore lv 15 + 3 defence_potions), `combat_chemist` (herblore lv 25 + 5 strength_potions), `master_herbalist` (herblore lv 45 + 3 super_attack_potions), `supreme_brewer` (herblore lv 65 + 3 super_strength_potions). (3) Added 5 Herblore skill-level achievements to `game/data/achievements.data.ts`: `herb_10` (+5% herblore XP), `herb_25` (+8%), `herb_50` (+10%), `herb_75` (+12%), `herb_99` (+5% all_skills) — mirroring the cooking achievement set exactly.
- Why it was improved: Session 9 flagged `Math.random()` in combat as the only remaining violation of the project's core determinism rule ("No randomness without seeded RNG"). With the fix, the entire tick pipeline is now fully deterministic — offline progress simulation and live ticks produce identical sequences given the same initial seed. The Herblore quests and achievements close the narrative and progression gap for the newest skill: Herblore had recipes, brewed items, and auto-drink logic but zero quest milestones or XP incentives, making the level arc invisible to players.
- Strategic direction chosen: Two parallel vectors — one architectural (complete the determinism contract) and one content (match the narrative coverage of every other skill). Both changes were zero-abstraction: the RNG infrastructure was already wired in for skills/drops/loot, and the quest/achievement pattern was copy-replicable from the cooking chain with no new types or handlers needed.
- Next potential vector: Add a combined Super Combat Potion (grants attack + strength + defence buffs simultaneously at a premium multi-herb cost), or add a Prestige system for skills at level 99 (reset skill for a permanent passive XP multiplier) to give endgame players a long-term goal.

## 2026-03-16 (automated) — session 10
- What was improved: Added the Herblore skill — a complete potion-brewing system that closes the herb → potion → combat stat buff loop. Herblore has 5 recipes (Lv1–65): Attack Potion (guam herb, +15 attack), Defence Potion (marrentill, +15 defence), Strength Potion (tarromin, +25 strength), Super Attack Potion (harralander, +20 attack), and Super Strength Potion (ranarr, +30 strength). Herbs drop as bag items from woodcutting and fishing drop tables. `processHerbloreTick` consumes herbs from the bag and adds potions, following the same pattern as `processCookingTick`. Potion buff types (`PotionBuffType`, `PotionBuff`) were added to `CombatState`, with `getPotionBonus()` wired into `getPlayerAttack/Strength/Defense` so brewed potions provide real combat value. Auto-drink (`autoDrink` toggle) was added to `CombatState` — when enabled, potions are automatically consumed at the start of each combat tick for each buff type not already active. Manual `drinkPotion` and `toggleAutoDrink` actions exposed through combatSlice. `HerbloreRecipeSelector` modal added to SkillsScreen. A potions card with active buff timers, manual drink buttons, and the auto-drink toggle was added to CombatScreen. Save migrated v18→v19 with herblore skill state and autoDrink/potionBuffs combat fields.
- Why it was improved: Fishing → Cooking → Auto-Eat formed a complete idle combat supply chain, but the herb drops from woodcutting and fishing drop tables had no downstream use. Herblore completes the second combat prep pipeline: herbs brewed into potions grant temporary attack/strength/defence bonuses, letting players meaningfully boost combat performance without manual micro-management via auto-drink.
- Strategic direction chosen: Close the idle automation chain one more step — herbs were already dropping, the PotionBuff infrastructure already existed in CombatState, and the processing-skill pattern (tick function + active recipe + bag-to-bag conversion) was already established by cooking. Herblore required zero new abstractions, only consistent application of existing patterns.
- Next potential vector: Add Herblore quests and achievements (parallel to the cooking quest/achievement chains). Alternatively, expand the potion set with additional buff types, or add a combined Super Combat Potion that grants all three stat buffs at once for a premium herb cost.

## 2026-03-16 (automated) — session 9
- What was improved: Refined the freshly added Auto-Eat system so it is fully usable and safer. The Combat screen now always shows the Food card, including an empty-state prompt when no cooked food is stocked, an Auto-Eat toggle, and threshold preset buttons at 25% / 50% / 75% HP. `useGameActions` now exposes the new auto-eat store actions for API consistency. In combat logic, auto-eat now prefers the smallest food that satisfies the threshold and only falls back to the largest heal when necessary. Manual `eatFood` no longer consumes food at full HP, and auto-eat no longer triggers after HP has already dropped to zero.
- Why it was improved: Session 8 shipped the first auto-eat pass, but it still left the vector incomplete and slightly unsafe. The threshold state existed without a way to configure it from the UI, the Food card vanished entirely when the bag was empty, and the tick ordering allowed a lethal hit to be healed afterwards. That made the system functional, but not yet stable or player-friendly.
- Strategic direction chosen: Refine the incomplete combat automation path before starting another feature. The goal of this pass was to make the existing fishing → cooking → combat loop legible, efficient, and behaviorally correct rather than stacking more systems on top of an unfinished implementation.
- Next potential vector: Replace the remaining `Math.random()` crit rolls in [`/Users/tobiasheidejensen/Projekter/Work/idle/oizys-keres/game/logic/combat/tick.ts`](/Users/tobiasheidejensen/Projekter/Work/idle/oizys-keres/game/logic/combat/tick.ts) with the seeded RNG path already used elsewhere, so combat fully obeys the project’s deterministic tick rule.

## 2026-03-16 (automated) — session 8
- What was improved: Added a passive Auto-Eat system to the combat loop. A toggle switch ("Auto-Eat") was added to the food panel in CombatScreen, mirroring the Auto-Fight card pattern. When enabled, `processCombatTick` automatically eats the best available food from the bag (highest healAmount first) whenever player HP drops below 50% after taking enemy damage — repeating until HP is above the threshold or food is exhausted. The logic runs inside `tryAutoEat()`, called within the enemy-attack branch of the combat tick loop before the death check, so it applies correctly to both live sessions and offline progress simulation. New fields `autoEat: boolean` and `autoEatThreshold: number` were added to `CombatState`. Pure command functions `toggleAutoEat` / `setAutoEatThreshold` were added to combat commands. Store actions and selector hooks were wired through. Save migration v17→v18 initialises `autoEat: false` and `autoEatThreshold: 0.5` for existing saves.
- Why it was improved: Session 7 shipped manual eating via "Eat" buttons in the food panel but explicitly identified auto-eat as the logical next vector. Players running auto-fight mode had to manually intervene to eat during long farming sessions, breaking the idle fantasy. The auto-eat toggle removes that friction: once enabled, the player can leave combat running indefinitely and food from their bag is consumed automatically to prevent deaths, creating a complete idle combat loop (auto-fight + auto-eat + auto-cook pipeline).
- Strategic direction chosen: Extend the idle automation loop one layer at a time. The tick architecture already had full `GameState` (including bag) available in `processCombatTick`, so no architectural changes were needed — the feature was purely additive. The 50% threshold default is intentionally conservative to avoid wasting food on minor chip damage, and the threshold is stored in state for future UI slider expansion.
- Next potential vector: Add an HP threshold slider to the Auto-Eat row (allow players to set 25%, 50%, 75%) so high-HP tanks can tune food efficiency. Alternatively, add a cooking auto-produce setting that prioritises cooking fish when bag has food below a quantity threshold, closing the full automation chain from fishing → cooking → combat.

## 2026-03-16 (automated) — session 7
- What was improved: Added Cooking as a full processing skill. Cooking converts raw fish resources (from the Fishing skill) into cooked food bag items that restore HP during combat. 7 recipes mirror the 7 fishing spots: Cook Shrimp (lv1, +3 HP) through Cook Shark (lv80, +20 HP). Each recipe consumes 1 raw fish per action and produces 1 cooked food item directly into the bag. `processCookingTick` is modeled after crafting automation — called directly from the main `processTick` loop, excluded from the standard skill loop, supporting both active (100%) and automated (50%) training. A `CookingRecipeSelector` modal (identical in structure to `FishingSpotSelector`) was added to SkillsScreen. A food panel was added to CombatScreen showing all food in bag with heal amounts and "Eat" buttons (`eatFood` action in combatSlice). 5 cooking quests span the full level arc (Fire It Up → Shark Fin Feast). 5 achievements grant Cooking XP multipliers at levels 10/25/50/75/99, with level 99 granting +5% all-skills XP. Save migration v16→v17 initialises cooking skill state. The `CATEGORY_ORDER` sort map in bag.ts was updated to include `food` between material and misc.
- Why it was improved: Fishing was added last session explicitly to lay the foundation for a cooking system. Without cooking, raw fish accumulate as an inert resource with no downstream use, and combat HP restoration had no player-driven mechanic. Cooking closes the fishing→cooking→combat loop: players gather fish, process them into food, then consume food to survive harder enemies. This adds a second meaningful resource sink (after crafting) and gives players a concrete reason to train Fishing beyond XP.
- Strategic direction chosen: Complete the fishing→cooking→combat loop as a single cohesive feature rather than shipping cooking without food consumption or vice versa. All architectural patterns (processing tick, bag item production, food consumption action) were derived from existing crafting automation and combat patterns with zero new abstractions.
- Next potential vector: Add a passive auto-eat system (toggle on CombatScreen) that automatically eats the best food when HP drops below a threshold, removing the manual micro-management burden during automated farming sessions.

## 2026-03-16 (automated) — session 6
- What was improved: Added Fishing as a full gathering skill following the exact same architecture as Woodcutting (tree tiers) and Mining (rock tiers). 7 tiered fishing spots unlock progressively from level 1 (Shallow Pond → raw shrimp) through level 80 (Abyssal Trench → raw shark). Fish are stored as resources (not bag items) for unlimited accumulation. A `FishingSpotSelector` modal mirrors the `RockSelector` pattern exactly. Two rare bag drops were added: pearl (4% at level 10+) and oyster (1.5% at level 35+). Fishing integrates into the existing deterministic tick system with automation unlocking at level 20. A 5-quest chain covers the full fishing progression arc. 5 achievements reward XP multipliers at levels 10/25/50/75/99. Save migration v15→v16 initialises fishing skill state and all 7 fish resources for existing saves.
- Why it was improved: The game had 4 skills (woodcutting, mining, crafting, summoning) but no 5th gathering skill to diversify the resource economy. Fishing follows the same idle pattern as the two existing gathering skills, giving players a meaningful new progression axis without introducing architectural complexity. It also lays the foundation for a future cooking system that will produce consumable food items for combat HP restoration — a long-requested gameplay depth layer.
- Strategic direction chosen: Extend the gathering loop with a clean, pattern-following skill before adding any new system. All architectural pieces (tiered resource overrides in tick.ts, selector modal UI, resource state, save migrations) already existed in identical form for woodcutting and mining. The work was purely additive: new data files, types, and UI wiring with zero changes to existing system behavior.
- Next potential vector: Add a Cooking skill that converts raw fish into cooked food items (bag items with a `healAmount` property), then wire food consumption into CombatScreen so players can eat during or between fights for HP restoration. Alternatively, add Woodcutting/Mining forge sigils to the Shop (woodcutting II adamantite sigil is currently missing) for symmetry.

## 2026-03-16 (automated) — session 5
- What was improved: Added an Ore Exchange upgrade tier to the ShopScreen. Players can now spend mithril_ore and adamantite_ore to purchase six permanent XP sigils: Mining I (150 mithril → +8% mining XP), Mining II (100 adamantite → +12% mining XP), Woodcutting I (150 mithril → +8% woodcutting XP), Combat Forge I (150 mithril → +8% crafting XP), Warlord II (100 mithril + 75 adamantite → +12% all-skills XP), and Grand Sigil of Wisdom (200 mithril + 100 adamantite → +5% all-skills XP). The shop type system was extended with a `resource` pricing kind (costs: ResourceCost[]) and a `grant_multiplier` effect kind. The shop logic routes resource-priced offers through a dedicated path that validates ore availability, deducts on purchase, blocks re-purchase (one-time, idempotent via multiplier ID), and calls `addMultiplier`. The ShopScreen gains a top "Ore Exchange" section with per-offer cost breakdowns colour-coded green when met, a ✓ Owned badge for purchased sigils, and a "Forge" CTA. A purple "+N% permanent XP bonus" tag is shown below the description.
- Why it was improved: Mithril ore and adamantite ore accumulated with limited downstream demand after crafting gates were saturated. Multiple prior sessions flagged this as the next vector — the ore economy loop needed a permanent-upgrade sink so late-game gathering felt purposeful rather than idle overflow. The existing multipliers system (additive/multiplicative, target per skill or all_skills) was already wired into XP calculations, making the integration clean.
- Strategic direction chosen: Convert existing ore surplus into permanent progression leverage through the shop rather than adding new mechanics. The data layer (multipliers state, addMultiplier, getEffectiveMultiplier) was already production-ready; only the pricing pathway and UI surface were new.
- Next potential vector: Add a Prestige or Mastery system for skills at level 99 (reset skill for a permanent passive bonus), or add a Summoning pet upgrade path using spirit_essence as a currency (e.g., permanent pet stat sigils).

## 2026-03-16 (automated) — session 4
- What was improved: Added a full Achievements screen with category browsing, progress bars, unlock dates, and reward hints. The screen organizes all achievements into collapsible sections (Progression, Skills, Collection, Secret), displays unlocked entries with a gold border and checkmark, shows partial progress bars on cumulative achievements (kill counts, quests completed, resource totals), and surfaces reward values even on locked achievements to motivate players. The StatsScreen gained an achievement progress card at the top that acts as an entry point (taps navigate to /achievements). Also added 14 new late-game achievement definitions covering: player levels 50/75/100, skill levels 75 for all four skills, kill milestones 2000 and 10000, combat level 100, spirit essence hoard (500), and quest legend (100 quests completed). `useAchievements` is now exported from `store/index.ts` for clean consumption.
- Why it was improved: Players could earn achievement rewards and see bonus effects in the StatsScreen multiplier panel, but had no way to browse their achievement state, understand what was locked, or see how far along cumulative achievements they were. The game loop lacked a key feedback surface for long-term progression goals. Late-game achievements were also missing — the highest thresholds previously stopped at player level 25 and skill level 50.
- Strategic direction chosen: Close the achievement visibility gap before adding new mechanics. The data layer (definitions, state, event handlers) was already production-ready; only the UI surface was missing.
- Next potential vector: Add a Prestige or Mastery system for skills at level 99, or introduce a currency-sink Shop upgrade tier that converts late-game ore stockpiles into permanent bonuses.

## 2026-03-16 (automated) — session 3
- What was improved: Expanded the quest system with 22 new quests across four categories: (1) Mid/late combat quest chain — 12 quests from `crypt_clearer` (kill 10 skeletons) through `elder_nemesis` (kill 3 elder demons) covering every zone from the Ancient Crypt to the Abyssal Depths, each rewarding relevant resources or equipment items (mithril_sword, runic_helmet, rune_sword, arcane_warblade) at appropriate milestones. (2) Mining tier chain — 5 quests from `copper_vein` through `adamantite_lord` gating on each mining level threshold (10/25/40/55/70) and requiring collection of the matching tier ore, rewarding mining XP boosts. (3) Summoning progression chain — 3 quests (`awakened_bond`, `ascending_spirit`, `mythic_pact`) covering summoning levels 18/30/60 and spirit essence accumulation, extending the summoning narrative through all pet evolution stages. (4) Two new daily quests: `daily_goblin_hunt` (8 goblins, unlocks at player level 6) and `daily_undead_hunter` (6 skeletons, unlocks at player level 12) to give mid-game players a daily combat objective.
- Why it was improved: The quest system previously ended at `goblin_intel` (~level 15 equivalent) while combat extended to 73+ level enemies (elder demon), mining had 6 unlockable rock tiers, and summoning had 5 pets and 4 evolution stages — all with zero quest narrative. Players entering those systems had no milestone feedback, no directed rewards, and no progression anchors to motivate continued play in those zones.
- Strategic direction chosen: Saturate all major existing systems with quests before adding new mechanics — the architecture (quest handler, event bus, SKILL_ACTION/KILL events) was already in place. Data-only change; no runtime behavior altered.
- Next potential vector: Add a "Sell Ore" offer to ShopScreen for mithril_ore and adamantite_ore at premium rates, creating an economic pressure valve for players with surplus high-tier ores. Or add a prestige/mastery system for skills at max level.

## 2026-03-16 (automated) — session 2
- What was improved: Added an `ActiveSessionCard` component to the Dashboard that surfaces the player's live game state at a glance. The card shows three possible active rows — training skill (skill name, level, dual progress bars for current action cadence and XP-to-next-level), active combat (enemy name, icon, and dual HP bars for player and enemy), and auto-craft (recipe name and craft progress). Each row is tappable and navigates to its corresponding screen. When nothing is active a soft idle prompt is shown. The card is placed immediately below the player level card so it reads first on the main screen.
- Why it was improved: The Dashboard previously required navigating to three different screens (Skills, Combat, Crafting) to understand what was currently running. In an idle game the home screen must function as mission control — players need instant confirmation that their session is progressing without any taps.
- Strategic direction chosen: Improve observability of existing systems before adding new mechanics. The data was already present in the store via `useActiveSkill`, `useCombatTracker`, and crafting automation state — this change surfaces it where it matters most.
- Next potential vector: Add mid/late-game quest chains for rock-tier mining (copper, iron, mithril, adamantite progression quests), late combat zones (ruins, dragon_lair, abyssal_depths kill quests), and summoning pet evolution quests to extend the quest narrative into systems already built but under-quested.

## 2026-03-16 (automated)
- What was improved: Added a "Nearest Goals" panel to the CraftingScreen infrastructure summary card. The panel computes the 3 closest locked equipment/weapon/tool recipes — sorted by total remaining skill-level gap — and displays each with its primary blocking requirement (e.g. "Mining 30→55") and a per-recipe ore cost breakdown for key resources (copper_ore, iron_ore, coal, mithril_ore, adamantite_ore) showing current stock vs. required amount. Ore costs are color-coded green when met and grey when short. Infra-blocked recipes are sorted to the bottom so the most immediately actionable goals surface first.
- Why it was improved: Players had no way to scan which recipes were closest to being craftable or how much mining output they needed to accumulate before unlocking the next gear tier. Without this information, the gap between gathering and crafting progression was opaque — especially for mithril/adamantite which require dedicated mining levels and long accumulation times.
- Strategic direction chosen: Surface actionable, forward-looking information inside the existing crafting summary card rather than adding a new screen, keeping the change focused and consistent with the "make system state legible" pattern established in recent sessions.
- Next potential vector: Add a "Sell Ore" offer to the ShopScreen for mithril_ore and adamantite_ore at premium rates so players with surplus high-tier ores can convert them to coins, creating an economic pressure valve and a coin-sink alongside the existing crafting sink.


## 2026-03-15 20:30:00 +0100
- What was improved: Added active bonus visibility to CraftingScreen and StatsScreen. The CraftingScreen infrastructure summary card now shows an "Active Bonuses" panel listing every bonus granted by built infrastructure — value, target name, and source. StatsScreen gained a new "Bonuses" section at the bottom that surfaces all active multipliers from any source (infrastructure, achievements, upgrades), formatted with green value highlights.
- Why it was improved: Players could not see what bonuses their built infrastructure was providing without browsing each recipe individually. The Master Forge, which costs 600 mithril ore and 200 adamantite ore, granted +15% mining and +15% crafting XP with no visible confirmation in the UI, making the payoff of the endgame resource grind invisible and weakening motivation to complete it.
- Strategic direction chosen: Surface existing system state more legibly rather than adding new mechanics — make every built structure immediately justify its cost through clear, persistent feedback.
- Next potential vector: Add a "Top Ore Demand" hint to the Dashboard or CraftingScreen showing the highest-level recipes and how many mithril/adamantite ore each requires, so players can plan their gathering goals before reaching the unlock thresholds.

## 2026-03-15 20:01:00 +0100
- What was improved: Corrected the endgame recipe gates so every adamantite-consuming recipe, including the Master Forge, now requires mining level 70, and updated the crafting screen to derive the infrastructure total from the live definitions so the new forge tier is counted correctly.
- Why it was improved: The late-game ore loop had already been expanded, but several recipes were still visible before adamantite could actually be mined and the summary UI still reported the pre-forge infrastructure cap, which undermined progression clarity.
- Strategic direction chosen: Refine the newly added late-game economy for internal consistency before adding more content, with emphasis on matching resource unlock thresholds to recipe requirements.
- Next potential vector: Expose forge bonuses and top-tier ore demand more explicitly in the UI so players can immediately understand the payoff of unlocking mithril and adamantite.

## 2026-03-15 19:30:00 +0100
- What was improved: Wired mithril_ore and adamantite_ore into the full crafting progression. All mithril-tier armor and weapons now consume mithril_ore instead of iron_ore (mining req bumped to 55). Runic-tier gear now requires mithril_ore as primary metal (mining req 60–62). Arcane-tier gear and late accessories now require mithril_ore + adamantite_ore alongside coal (mining req 65–67). Added the Master Forge infrastructure (build_forge recipe), a permanent +15% mining XP / +15% crafting XP structure requiring mithril_ore 600 + adamantite_ore 200 to construct — the first concrete endgame sink for both high-tier ores.
- Why it was improved: The previous state had a naming/value mismatch: "mithril" gear used generic iron ore, and mithril_ore/adamantite_ore accumulated without any meaningful downstream demand. The Master Forge creates a concrete production goal that motivates players to grind the mithril and adamantite tiers instead of idling on iron/coal.
- Strategic direction chosen: Close the ore economy loop before adding new systems — make every mining tier yield resources that matter to a specific crafting band.
- Next potential vector: Add shop sell offers for mithril_ore and adamantite_ore so players can convert surplus ores to coins, or expand crafting into consumable/buff items that use high-tier ores as fuel.

## 2026-03-15 18:45:27 +0100
- What was improved: Routed mid- and late-game crafting recipes to copper ore, iron ore, and coal instead of a single generic ore pool, added ore stockpile achievements for coal/mithril/adamantite progression, and expanded the dashboard resource summary to surface newly gathered resource tiers.
- Why it was improved: Mining tier unlocks existed, but downstream progression still flattened most crafting demand into generic ore and hid advanced resource stocks from the main UI, which weakened the economy and made high-tier gathering feel disconnected.
- Strategic direction chosen: Deepen the existing gathering-to-crafting loop before adding more systems, with emphasis on making specialized resource tiers visible and economically meaningful.
- Next potential vector: Introduce late-game crafting or shop sinks for mithril and adamantite so the highest mining tiers feed concrete production goals instead of only achievement progression.

## 2026-03-15 16:20:35 +0100
- What was improved: Hardened gathering specialization state by introducing typed tree/rock tier IDs, normalizing invalid selections on load and admin level changes, and exposing mining target selectors alongside woodcutting in the store/UI.
- Why it was improved: The mining rock tier system was already shipped, but persisted or down-leveled selections could drift out of sync with the player’s actual level and the UI was still doing raw lookups instead of using normalized selectors.
- Strategic direction chosen: Stabilize progression systems before expanding them further, with emphasis on type-safe data-driven state and deterministic save repair.
- Next potential vector: Connect the new tier-specific ores into crafting recipes, shop valuation, and progression unlocks so mining tiers affect the broader economy.

## 2026-03-15 16:14:49 +0100
- What was improved: Added mining rock tiers, tier-specific ore resources, save migration for `activeRockId`, and the mining rock selector UI.
- Why it was improved: Mining needed progression parity with woodcutting so players could make longer-term gathering decisions instead of training against a single static resource profile.
- Strategic direction chosen: Expand core gathering progression with data-driven specialization rather than adding new surface-level systems.
- Next potential vector: Normalize selected gathering targets and integrate the new ore outputs into downstream crafting/economy systems.
- Source: commit `ec48450a3d47f03ca9657be9bf3c7f1afc04961c`

## 2026-03-16 — Offline Progress Summary
- What was improved: Added full offline progress summary UX. Fixed `summarizeOfflineProgress` which was silently dropping all resource gains, item drops, and combat kills from the offline summary. Added `offlineSummary` as transient store state computed at boot, then built `OfflineProgressModal` that shows on Dashboard after being away 60+ seconds — displays skill XP gained, level-ups (highlighted), resources collected, items found, and enemies defeated.
- Why it was improved: The offline tick engine already processed progress correctly but showed players nothing — a core idle-game mechanic was completely invisible. Players returning after hours away had no feedback about what happened, which breaks the game feel of idle progression.
- Strategic direction chosen: Improve player feedback and session loop before expanding systems further. The modal gives clear value to each return visit and makes offline progress tangible.
- Next potential vector: Add an "offline efficiency" multiplier or cap visible in the modal to motivate players to upgrade infrastructure for better idle returns. Or expand mid/late quest chains (current last combat quest is goblin_intel at ~level 15 equivalent).
